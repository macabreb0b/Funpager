import React from 'react';

const Page = ({ page, pageId, fetchPage }) => {
    return(
        <div className="single-bench-show">
            <h2> Im a page </h2>
        </div>
    );
};

export default Page;